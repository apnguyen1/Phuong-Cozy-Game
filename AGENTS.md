# Project instructions

Read `docs/tickets/README.md`, the assigned `docs/tickets/PCW-XX.md`,
`docs/design/map-proposal-v0.2.md`, and `docs/design/visual-direction-v0.1.md`.
The September 24 request authorizes building the verification system. Earlier
no-code notes describe the design task; they do not prohibit this harness. Game
implementation and publishing remain separate from this task.

Roblox, mobile/tablet/desktop, 8–10 real participants, independent third-person
cameras, one detailed bedroom, and shared six-quest progress are the current
direction. Do not restore the historical Phaser/browser or NPC-party assumptions.

For ticket work, follow `verification/README.md`. Builders deliver the assigned
ticket's files and evidence, then hand them to an independent verifier. Builders
must not approve their own work, invent palette values, weaken acceptance criteria,
or describe reference art as production-ready. Simplified visual detail is allowed;
required identity, color roles, interactions, and usability must remain.

Use the project `verifier` agent when the user requests verification by an agent.
The reusable agent is defined in `.codex/agents/verifier.toml`; its instructions
also work as a manual reviewer brief if custom roles are unavailable in a session.
Verification does not publish, upload, or overwrite a Roblox place.

The personal birthday note remains user-authored. Do not generate or paraphrase it.
